{
  "scripts": {
    "dev": "vite",
    "build-login": "BUILD=login vite build",
    "build-app": "BUILD=app vite build",
    "build-app-test": "BUILD=app TEST_MODE=1 vite build",
    "build-all": "npm run build-login && npm run build-app",
    "build-all-test": "npm run build-login && npm run build-app-test",
    "prepare-test": "npm run build-all-test && cargo build --features test-mode",
    "lint": "tsc --noEmit && oxlint",
    "lint:fix": "tsc --noEmit && oxlint --fix",
    "test:rust": "cargo test --features test-mode --tests -- --test-threads=1",
    "test:web": "playwright test",
    "test:all": "npm run test:rust && npm run test:web"
  },
  "type": "module",
  "devDependencies": {
    "@atlaskit/pragmatic-drag-and-drop": "^1.7.7",
    "@atlaskit/pragmatic-drag-and-drop-hitbox": "^1.1.0",
    "@codemirror/autocomplete": "^6.20.0",
    "@codemirror/lang-markdown": "^6.5.0",
    "@codemirror/language-data": "^6.5.2",
    "@codemirror/state": "^6.5.4",
    "@codemirror/view": "^6.39.11",
    "@playwright/test": "^1.58.0",
    "@simplewebauthn/browser": "^13.2.2",
    "@types/node": "^25.1.0",
    "codemirror": "^6.0.2",
    "heic-to": "^1.3.0",
    "lightningcss": "^1.31.1",
    "oxlint": "^1.42.0",
    "typescript": "^5.9.3",
    "valibot": "^1.2.0",
    "vite": "8.0.0-beta.10"
  }
}
